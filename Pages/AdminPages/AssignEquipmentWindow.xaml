<Window x:Class="fitness_club.Pages.AdminPages.AssignEquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fitness_club.Pages.AdminPages"
        mc:Ignorable="d"
        Title="AssignEquipmentWindow" Height="450" Width="800">
     <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="RemainingTextBlock" FontWeight="Bold" FontSize="14" Margin="0 0 0 10"
                   Text="Доступно к распределению: N шт."/>

        <DataGrid x:Name="HallGrid" Grid.Row="1" AutoGenerateColumns="False"
                  CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Зал" Binding="{Binding HallName}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Сохранить" Grid.Row="2" HorizontalAlignment="Right"
                Width="150" Margin="0 15 0 0" Click="SaveButton_Click"/>
    </Grid>
</Window>
